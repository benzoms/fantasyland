<style>
    body{
       background-image: url("../images/gifs/forestclearing.gif");
       overflow: hidden;
    }
    #wizardfrog {
    position: absolute;
    bottom: 15%;
    left: 50vw;
    height: 450px;
    width: 450px;
    
    animation: boing 2s ease-in-out infinite alternate;
    transform: translate(-50%,-50%);
}
#wizardfrog img{
    height: 450px;
    width:450px;
    transform: scale(2);
}
@keyframes boing {
    from {
      transform: scale(1, 1) translate(-50%, -20px);
    }
    to {
      transform: scale(1, 1) translate(-50%, 0px);
    }
  }

.slideshow-container {
    position: absolute;
    
    transform: translate(-50%,20%);
}
.text {
text-shadow: -1px 0 black, 0 1px black, 1px 0 black, 0 -1px black;
font-family: "Prompt", sans-serif;
  font-weight: 500;
  font-style: normal;
  color: #f2f2f2;
  font-size: 25px;
  padding: 8px 12px;
  position: absolute;
  bottom: 0;
  width: auto;
  text-align: center;
  background-color: rgba(0,0,0,0.5);
  overflow-x:hidden;
  
}

/* Number text (1/3 etc) */
.numbertext {
    text-shadow: -1px 0 black, 0 1px black, 1px 0 black, 0 -1px black;
font-family: "Prompt", sans-serif;
  font-weight: 500;
  font-style: normal;
  color: #f2f2f2;
  font-size: 20px;
  padding: 8px 12px;
  position: absolute;
  top: 0;
}

.prev, .next {
  cursor: pointer;
  position: absolute;
  top: 50%;
  width: auto;
  padding: 16px;
  margin-top: -22px;
  color: white;
  font-weight: bold;
  font-size: 30px;
  transition: 0.6s ease;
  border-radius: 0 3px 3px 0;
  user-select: none;
}
.next {
  right: 0;
  border-radius: 3px 0 0 3px;
}

.dot {
  cursor: pointer;
  height: 15px;
  width: 15px;
  margin: 0 2px;
  background-color: #bbb;
  border-radius: 50%;
  display: inline-block;
  transition: background-color 0.6s ease;
}

.active, .dot:hover {
  background-color: #717171;
}

/* Fading animation */
.fade {
  animation-name: fade;
  animation-duration: 1.5s;
}

@keyframes fade {
  from {opacity: .4} 
  to {opacity: 1}
}
.speech-bubble {
    bottom: 750px;
	position: absolute;
	background: rgba(255,255,255,0.8);
	border-radius: .4em;
    width:80vw;
    transform: translate(-25%, 0%);
}
#frogaskform{
    position: absolute;
    animation: none;
    bottom:5svh;
    transform: translate(-50%, 0%);  
}
#frogaskform textarea{
    width: 60svw;
    font-size: 25px;
    
    background-color: rgba(237, 159, 102,0.5);
    
    padding: 10px 10px;
    box-sizing: border-box;
    border-radius: 10px;
    color: #eeedbe;
    border-color: #ed9f66;
    border-width: 5px;

    font-family: "Quicksand", sans-serif;
    font-optical-sizing: auto;
    font-weight: 600;
    font-style: normal;
}

#submittopic {
    margin-top:5px;
    width: 60svw;
    font-size: 30px;

    font-family: "Quicksand", sans-serif;
    font-optical-sizing: auto;
    font-weight: 700;
    font-style: normal;  

    background-color: #bd733d;
    border-radius: 10px;
    color: #eeedbe;
    border-color: #ed9f66;
    border-width: 5px;
}
.speech-bubble:after {
	content: '';
	position: absolute;
	bottom: 0;
	left: 50%;
	width: 0;
	height: 0;
	border: 20px solid transparent;
	border-top-color: rgba(255,255,255,0.8);
	border-bottom: 0;
	border-left: 0;
	margin-left: -10px;
	margin-bottom: -20px;
}

.speech-bubble h2{
    margin: 15px;
}
#wizardfrog h2 {
  font-family: "Walter Turncoat", cursive;
  font-weight: 400;
  font-style: normal;
  font-size:30px;
}
#mapicon {
  display:none;
}
</style>



<div id="wizardfrog">
    <hgroup class="speech-bubble">
	    {{!-- <h1>Bubbly</h1> --}}
        {{#each frogadvice}}
            <h2> {{this}}</h2>
        {{/each}}
    </hgroup>
    <img src="../images/assets/animals/bearidle2.gif">
</div>
<div id="frogaskform">
<form action="/sage-advice2" method="POST">
    <textarea id="adviceTopic" name="topic" rows="4" cols="50">How do I stay motivated?</textarea>
    <br>
    {{#if frogresting}}
    <input id="submittopic" type="submit" value="Ask Bear" disabled>
    {{else}}
    <input id="submittopic" type="submit" value="Ask Bear">
    {{/if}}
    
</form>
</div>


<script>
/*document.addEventListener('DOMContentLoaded', () => {
    document.getElementById('submittopic').addEventListener('click', () => {
        document.getElementById("submittopic").disabled = true;   
    });
});*/
async function askfrog(){
    alert(document.getElementById('adviceTopic').value);
}

let slideIndex = 1;
showSlides(slideIndex);

function plusSlides(n) {
  showSlides(slideIndex += n);
}

function currentSlide(n) {
  showSlides(slideIndex = n);
}

function showSlides(n) {
  let i;
  let slides = document.getElementsByClassName("mySlides");
  let dots = document.getElementsByClassName("dot");
  if (n > slides.length) {slideIndex = 1}    
  if (n < 1) {slideIndex = slides.length}
  for (i = 0; i < slides.length; i++) {
    slides[i].style.display = "none";  
  }
  for (i = 0; i < dots.length; i++) {
    dots[i].className = dots[i].className.replace(" active", "");
  }
  slides[slideIndex-1].style.display = "block";  
}
</script>